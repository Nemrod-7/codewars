#include <iostream>

using namespace std;

int main () {

  pair<int,int> p = {0,0};
  auto &[x,y] = p;

  int cnt = 0;

  for (cnt = 0, y = 0; y < 8; y++) {
      for (x = 0; x < 6; x++) {
          cout << '.';
      }
      cout << endl;
  }

}
